import { FlyerDesignerContextType } from "../../../src/context/FlyerDesignerContext";

export const mockFlyerDesignerContextReturnObj: FlyerDesignerContextType = {
  selectedFlyer: null,
  setSelectedFlyer: () => {},
  selectedSection: "top_backgroundColor",
  setSelectionSection: () => "top_backgroundColor",
};
